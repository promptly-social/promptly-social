# Example environment variables for local testing of user_activity_analysis
# Copy this file to .env and fill in your actual values

# Cloud SQL Database Configuration
CLOUD_SQL_INSTANCE_CONNECTION_NAME=your-project:region:instance-name
CLOUD_SQL_DATABASE_NAME=your-database-name
CLOUD_SQL_USER=your-db-user
CLOUD_SQL_PASSWORD=your-db-password

# AI Service Configuration
OPENROUTER_API_KEY=your-openrouter-api-key

# Optional: Google Cloud credentials (if not using default application credentials)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Optional: Override default configuration values
# POST_THRESHOLD=5
# MESSAGE_THRESHOLD=10
# BATCH_TIMEOUT_MINUTES=15
# MAX_USERS_PER_BATCH=100
